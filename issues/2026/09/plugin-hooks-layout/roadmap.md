```yaml
status: complete
branch: issue/plugin-hooks-layout
last-updated: 2026-09-16
next-step: ""
github-issue: "#36"
```

## Phase 1: Expose the defect

- [x] 1.1 Add a `test(...)` to `tests/customizations.test.mjs` named `plugin layout is Claude format so VS Code expands ${CLAUDE_PLUGIN_ROOT} (#36 plugin-hooks-layout)` that asserts: `.claude-plugin/plugin.json` exists and parses; root `plugin.json` and root `hooks.json` do not exist; the manifest's `hooks` equals `./hooks/hooks.json` and that file exists; every hook command in it starts with `${CLAUDE_PLUGIN_ROOT}/` and, after replacing the token with the repo root, is an existing executable file — verify: `node --test tests/customizations.test.mjs; echo "exit=$?"` prints `exit=1` and the TAP output names exactly that test as `not ok` (every other test still `ok`)

## Phase 2: Move the manifest and hook wiring

- [x] 2.1 `git mv plugin.json .claude-plugin/plugin.json`, then change only the `hooks` field to `"./hooks/hooks.json"` (all other fields — `name`, `description`, `version`, `author`, `homepage`, `repository`, `license`, `keywords`, `agents`, `commands` — unchanged; no `$schema`) — verify: `node -e 'const p=require("./.claude-plugin/plugin.json");const o=JSON.parse(require("child_process").execSync("git show origin/main:plugin.json"));for(const k of Object.keys(o))if(k!=="hooks"&&JSON.stringify(p[k])!==JSON.stringify(o[k]))process.exit(1);if(p.hooks!=="./hooks/hooks.json"||"$schema"in p)process.exit(1)'` exits 0 and `test ! -e plugin.json`
- [x] 2.2 `git mv hooks.json hooks/hooks.json` with no content change — verify: `git diff --quiet origin/main:hooks.json HEAD:hooks/hooks.json` (after commit) or `cmp <(git show origin/main:hooks.json) hooks/hooks.json` exits 0, and `test ! -e hooks.json`
- [x] 2.3 Retarget the existing "plugin manifest…" test in `tests/customizations.test.mjs` to read `.claude-plugin/plugin.json` and to resolve `plugin.hooks` relative to the repo root (strip a leading `./`) so the version-match and `checkHooks` assertions cover the new files — verify: `node --test tests/customizations.test.mjs` exits 0 with the #36 test now `ok`

## Phase 3: Prove the token is expanded at runtime

- [x] 3.1 (manual) In your **user** `settings.json` (not workspace): under `chat.pluginLocations` set the `~/DP/agento` clone entry to `false` and add `"/home/david/DP/agento-worktrees/plan-20260915-192647": true`; run *Developer: Reload Window*; open a **non-Agento** repository in a new window; start a new chat and ask the agent to run `git status`; then tell the Builder which repository and the approximate time. The Builder finds the newest `~/.config/Code/logs/*/window*/exthost/GitHub.copilot-chat/GitHub Copilot Chat Hooks.log` containing that repository's path, copies the SessionStart and PreToolUse `Running:` / `Completed` / `Output:` lines into `evidence/step-3-1-runtime-hooks.md`, and links it here. Afterwards remove the worktree entry and restore the clone entry to `true` (the clone gains the new layout when `main` is pulled after ship) — verify: in that log, both `Running:` commands are `/home/david/DP/agento-worktrees/plan-20260915-192647/scripts/hooks/session-context.sh` and `…/delivery-guard.sh` (absolute, no `${CLAUDE_PLUGIN_ROOT}`), each `Completed (Success)`, `grep -c 'not found'` is 0, exactly one plugin `Executing 1 hook(s)` per SessionStart and per tool call, and the SessionStart output contains `Agento CLI: node /home/david/DP/agento-worktrees/plan-20260915-192647/scripts/agento.mjs`; the chat also lists `/agento delivery-status` and the Agento agents (added 2026-09-15: with the user's explicit permission the Builder performed the settings part itself — backup at `~/.config/Code/User/settings.json.agento-3-1.bak`, `chat.pluginLocations` now `/home/david/DP/agento: false` + this worktree `true`, `chat.plugins.enabled: true` — and opened `/home/david/DP/prismicon` (has `.github/agento.json`, no workspace `.github/hooks/`) with `code --new-window` at 17:35 EDT; the chat/`git status` part and the report-back remain the user's) — completed 2026-09-15: user reported "done 6:01 est" (22:01 UTC); evidence [evidence/step-3-1-runtime-hooks.md](evidence/step-3-1-runtime-hooks.md) from `logs/20260910T224623/window22` — all six verify checks pass (both `Running:` commands absolute worktree paths, both `Completed (Success)`, `not found` count 0, one `Executing 1 hook(s)` per event, `Agento CLI:` line present); settings restored by the Builder afterwards (`chat.pluginLocations` back to `"/home/david/DP/agento": true`, worktree entry removed, file parses, backup `~/.config/Code/User/settings.json.agento-3-1.bak` kept)

## Phase 4: Docs and references

- [x] 4.1 Rewrite the dev-clone advice in `README.md` ("Verifying and troubleshooting", the sentence citing `chat.pluginLocations … false`), `docs/install.md` ("Set the value to `false`…" paragraph), and `AGENTS.md` (the "keep the plugin **disabled** for this workspace (`chat.pluginLocations`)" rule): recommend **not** registering the dev clone in `chat.pluginLocations` because the workspace-mode `.github/hooks/` wiring already guards the clone and every worktree and is the only wiring whose `./scripts/hooks/…` paths resolve inside a worktree; fallback when the clone must stay registered for other repos: disable the plugin per workspace from the Extensions view → *Agent Plugins – Installed* → context menu (or the Agent Customizations editor), in each worktree window too; state that `chat.pluginLocations` is machine-scoped (a workspace value is ignored) and remove the `"<path>": false` snippet — verify: `grep -rn 'pluginLocations' README.md docs/install.md AGENTS.md` shows no `false` toggle; `grep -c 'Agent Plugins' README.md docs/install.md AGENTS.md` is ≥ 1 for each file; `grep -n 'machine-scoped' README.md docs/install.md AGENTS.md` hits each file
- [x] 4.2 Update every old-layout reference: `AGENTS.md` layout bullet (`.claude-plugin/plugin.json` + `hooks/hooks.json`), `docs/hooks.md` opening sentence, `.github/agents/copilot-mechanic.agent.md` file list and "root `hooks.json`" bullet, `.github/prompts/agento-init.prompt.md` self-detection (`.claude-plugin/plugin.json` with `"name": "agento"`) and the "workspace or user `chat.pluginLocations`" phrase (user-level, machine-scoped), `.github/prompts/ship.prompt.md` release-entry diff (`-- .claude-plugin/plugin.json package.json`); copy the two prompts to `commands/agento-init.md` and `commands/ship.md` byte-identically — verify: `grep -rn 'plugin\.json\|hooks\.json' AGENTS.md docs/hooks.md .github/agents .github/prompts commands | grep -v '\.claude-plugin/plugin\.json\|hooks/hooks\.json\|agento\.json'` prints nothing; `cmp .github/prompts/agento-init.prompt.md commands/agento-init.md && cmp .github/prompts/ship.prompt.md commands/ship.md` exit 0
- [x] 4.3 Set `"version": "0.4.1"` in `.claude-plugin/plugin.json` and `package.json`; add `## 0.4.1 (unreleased)` as the first CHANGELOG.md entry with a bold-lead **Fixed** bullet describing the layout move, the runtime effect (plugin hooks now execute), the corrected dev-clone advice, and `#36` — verify: `node -e 'const p=require("./.claude-plugin/plugin.json"),k=require("./package.json");if(p.version!=="0.4.1"||k.version!=="0.4.1")process.exit(1)'` exits 0; `sed -n '3p' CHANGELOG.md` prints `## 0.4.1 (unreleased)`; `sed -n '3,12p' CHANGELOG.md | grep -c '#36'` ≥ 1

## Phase 5: Gate and publish

- [x] 5.1 Full gate against the recorded baseline (shellcheck exit 0 / 0 findings; 140 tests pass; guard smoke exit 0): `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh`, `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'`, `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` — verify: shellcheck exit 0 with no findings; tests report `# fail 0` with `# pass` ≥ 141; guard smoke exit 0; record all three results on this line — results (2026-09-15, a938abd): shellcheck exit 0, no findings; node --test `# tests 141`, `# pass 141`, `# fail 0` (baseline 140 + the #36 test); guard smoke exit 0
- [x] 5.2 Scope boundary and publish: `git diff --name-only origin/main...HEAD` lists nothing under `scripts/hooks/` or `.github/hooks/` and lists both `.claude-plugin/plugin.json` and `hooks/hooks.json` while `plugin.json` and `hooks.json` are deletions (`git diff --diff-filter=D --name-only origin/main...HEAD`); `git merge origin/main` (never rebase); push; set `status: in-review` — verify: `gh pr view --json mergeStateStatus --jq .mergeStateStatus` is not `BEHIND`/`DIRTY`; PR body starts with `Fixes #36` — results (2026-09-15, base `origin/main` d1bdc3e): changed-file list has 0 entries under `scripts/hooks/` or `.github/hooks/` and includes `.claude-plugin/plugin.json` and `hooks/hooks.json`; the strict `--diff-filter=D` command prints nothing because git pairs the moves as renames (`R090 plugin.json → .claude-plugin/plugin.json`, `R100 hooks.json → hooks/hooks.json`), while `git diff --no-renames --diff-filter=D --name-only origin/main...HEAD` prints `hooks.json` and `plugin.json` — the root files are gone; `git merge origin/main` → `Already up to date`; PR #37 `mergeStateStatus` = `BLOCKED` (draft awaiting review; not `BEHIND`/`DIRTY`), body starts with `Fixes #36`; plan.md `## Resolution` written

## Follow-ups

- File the VS Code bug upstream (microsoft/vscode): a Copilot-format (format 0: root `plugin.json` + root `hooks.json`) plugin's hook commands are spawned with `${CLAUDE_PLUGIN_ROOT}` unexpanded and without `CLAUDE_PLUGIN_ROOT` in the environment, although the format descriptor lists both `${PLUGIN_ROOT}` and `${CLAUDE_PLUGIN_ROOT}` as `pluginRootTokens` and the docs promise expansion. Minimal repro: a directory with `plugin.json` `{"name":"repro","version":"0.0.1","hooks":"hooks.json"}`, `hooks.json` `{"hooks":{"SessionStart":[{"type":"command","command":"${CLAUDE_PLUGIN_ROOT}/hello.sh","timeout":5}]}}`, and an executable `hello.sh` that echoes a JSON `additionalContext`; register it via `chat.pluginLocations`, start a chat → Hooks output channel shows `/bin/sh: 1: /hello.sh: not found`. Renaming to `.claude-plugin/plugin.json` + `hooks/hooks.json` fixes it. Source trace: `evidence/vscode-source-trace.txt` (format 0 `parseHooks → BTo(s,o,t,i)` drops the root URI; formats 1/2 go through `HTo` which substitutes). Observed on 1.132.0 `df53daabb18c`.
- Extend the delivery guard's `PROTECTED` regex to gate `hooks/hooks.json` and `.claude-plugin/` edits (a `scripts/hooks/` change — separate, approval-gated delivery with new `tests/guard-fixtures.txt` cases).
- Exercise install option B (*Chat: Install Plugin From Source*) and option C (`copilot plugin install`) against the new layout after release and record the result in docs/install.md.
