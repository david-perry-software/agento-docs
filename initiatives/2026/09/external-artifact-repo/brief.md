Source: inline argument to /agento new-initiative — 2026-09-15

I want to change it so that features/ issues/ and initiatives/ folders are actually in their own repo outside of the repo using the plugin. For example, Instead of those folders being inside the prismicon repo, there should be a new repo created during init that sets in the same directory as the prismicon repo called prismicons-demo
