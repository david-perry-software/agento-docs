```yaml
status: complete
branch: feature/mirrored-artifact-branches
last-updated: 2026-09-17
next-step: ""
initiative: "external-artifact-repo"
```

## Phase 1: Branch and session mirroring

- [x] 1.1 Create the mirrored artifact branch in the companion half when the Planner creates the product branch (re-scoped 2026-09-16 — the original tick only checked this product repo's own branch; no companion branch or plugin change existed, so it was unticked): `delivery-planner.agent.md` step 5 and `new-feature.prompt.md` / `new-issue.prompt.md` step 4 create `feature/<slug>` (or `issue/<slug>`) in the companion half right after the product branch — `git -C <companion.path> switch -c <branch>` from its detached `origin/<default>` HEAD, no upstream — and reject a companion half that is not detached or already carries that branch elsewhere; `start-session.prompt.md` build mode step 3 adds `--no-track` to the companion `worktree add -b <branch> … origin/<default>` (paired-artifact-worktrees Finding 3) — verify: `grep -c 'switch -c' .github/agents/delivery-planner.agent.md .github/prompts/new-feature.prompt.md .github/prompts/new-issue.prompt.md` ≥ 1 each; `grep -c 'no-track' .github/prompts/start-session.prompt.md` = 1; `diff .github/prompts/start-session.prompt.md commands/start-session.md` empty; on a /tmp product+companion pair, `git -C <companion> worktree add --no-track -b feature/x <half> origin/main` leaves `git -C <half> rev-parse --abbrev-ref @{upstream}` failing (no upstream) and `git -C <plan-half> switch -c feature/x` from detached `origin/main` yields `agento.mjs session` `companion.branch: "feature/x", detached: false`.
- [x] 1.2 Ensure the session record reports the promoted plan pair — product half and companion half both on `feature/<slug>` via the 1.1 promotion — as one logical delivery: same `delivery.slug` and `delivery.branch` from either half, `companion.branch` equal to `delivery.branch`, `worktrees[]` entries tagged `repo: product|companion`; add a `scripts/agento.test.mjs` test for a `plan-*` pair promoted from detached `origin/main` on both sides — verify: `node --test scripts/agento.test.mjs` exit 0 with the new test asserting `fromHalf.delivery` deep-equals `fromProduct.delivery` and `companion.branch === delivery.branch`.
- [x] 1.3 Add the companion PR to the CLI: `roadmap.md` gains the optional `artifact-pr: "#<n>"` header (delivery-artifacts.instructions.md, next to `github-issue`), `header()`-parsed into `artifactPr` on every describe record (`status`, `session.delivery`, `initiative` features, `next`); `session --pr` adds `companionPr` — `gh pr view <branch>` run in the companion clone (`cwd: artifactsRoot`), `null` in the in-repo layout, degrading with a `companionPr:` warning exactly like `pr` — without touching any branch — verify: `node --test scripts/agento.test.mjs scripts/session-state.test.mjs` exit 0 with new tests (in-repo: `companionPr: null` and no extra gh call; companion: two `gh pr view` invocations, one per repo, and `companionPr` populated; `artifactPr: "#7"` read from a header); `node scripts/agento.mjs session --pr` in this in-repo checkout prints `"companionPr": null`. (reviewed 2026-09-17: every surface named here is real except `initiative` features, which `deriveInitiative` does not project — carried by 1.6.)
- [x] 1.4 (added 2026-09-16) Confirm `resolve`/`find`/`next` already read a roadmap that exists only on the companion's `origin/<branch>` and carry its `artifactPr` — extend the existing "fall back to the companion's origin branch" test with an `artifact-pr` header and assert the record carries it while the product repo's same-named roadmap is ignored; `next` candidates gain `artifactPr` — verify: `node --test scripts/agento.test.mjs` exit 0 with the extended assertions.
- [x] 1.5 (added 2026-09-16) Make `session` and `next` read the delivery roadmap from the registered companion half in companion mode: today `allRoadmaps()` walks only the companion clone (on `<default>`), so a pair whose roadmap lives on the mirrored branch reports `delivery.roadmap: null`, `lifecycle: no-delivery`, and an `allowed[]` without the build/review commands (found while verifying 1.3 on a /tmp pair). When `companion.registered` and the half exists, roadmaps found in the half's working tree take precedence over the clone's same-path roadmaps; the in-repo layout is unchanged — verify: `node --test scripts/agento.test.mjs` exit 0 with a new test where the roadmap is committed only in the companion half and `session` from both halves reports `delivery.status: "in-progress"`, `delivery.artifactPr: "#7"`, `lifecycle: building`, `allowed` containing `/agento build-feature <slug>`, and `next` from either half emits `/agento build-feature <slug>`.
- [x] 1.6 (added 2026-09-17) Reconcile the `initiative` members claim with the CLI (review round 1, Finding 3): either add `artifactPr: roadmap?.artifactPr ?? null` to the member record in `deriveInitiative` (`scripts/agento.mjs`) with an assertion in the `initiative` test of `scripts/agento.test.mjs`, or remove `initiative` members from the `artifactPr` claims in CHANGELOG.md, docs/commands.md, delivery-artifacts.instructions.md, and plan.md acceptance item 3 — verify: `node scripts/agento.mjs initiative external-artifact-repo | grep -c artifactPr` is ≥ 1 (option A) or `grep -n 'initiative. members\|initiative. features\|`initiative`, `next`' CHANGELOG.md docs/commands.md .github/instructions/delivery-artifacts.instructions.md` names no `artifactPr` claim (option B); `node --test scripts/agento.test.mjs` exit 0. (done 2026-09-17, option A: member records carry `artifactPr`; `grep -c` → 7, `node --test scripts/agento.test.mjs` 49/49/0.)

## Phase 2: Planner, Builder, and Reviewer writes

- [x] 2.1 Teach the Planner (`delivery-planner.agent.md` steps 5, 7, 8; `new-feature.prompt.md` steps 4–5; `new-issue.prompt.md` steps 4, 7–8) to write `plan.md`/`roadmap.md`/`evidence/` under `agento.mjs paths` `artifactRoot` (the companion half in companion mode), commit them with `git -C <companion.path>`, push with upstream, publish the product branch with one empty Conventional Commit so the draft code PR can open, open a **draft** companion PR titled `docs(<type>): <slug>` whose body links the code PR, cross-link the code PR body with `gh pr edit`, and record `artifact-pr: "#<n>"` in the roadmap header (second companion commit); in-repo mode stays today's single-repo flow — verify: `grep -c 'docs(<type>): <slug>' .github/agents/delivery-planner.agent.md .github/prompts/new-feature.prompt.md .github/prompts/new-issue.prompt.md` ≥ 1 each; `grep -c 'artifact-pr' .github/agents/delivery-planner.agent.md` ≥ 1; `node --test tests/customizations.test.mjs` exit 0 (mirrors identical, links resolve, §N valid).
- [x] 2.2 Make the Builder's per-step commit two commits in companion mode — code in the product half, roadmap tick + evidence in the companion half (`git -C <companion.path>`), pushed together product-first — with the resume protocol fetching and integrating both halves (`origin/<branch>` and the companion default), the audit reading the roadmap from the companion half, the pause protocol committing and pushing both, and completion setting `status: in-review` in the companion; rewrite policy §3 (evidence path lives in the artifact checkout), §5 (baseline record lives in the companion plan.md), §7 (two-commit rule, integrate both defaults) and the concurrent-delivery integration bullets to merge the companion's `origin/<default>` into the companion branch alongside the product merge; `build-feature.prompt.md`, `build-issue.prompt.md`, `ap.prompt.md`, and `delivery-autopilot.agent.md` read roadmap state from the companion half — verify: `grep -c 'companion' .github/agents/delivery-builder.agent.md .github/instructions/delivery-policy.instructions.md .github/instructions/concurrent-delivery.instructions.md .github/agents/delivery-autopilot.agent.md` ≥ 1 each; `node --test tests/customizations.test.mjs` exit 0; `diff <(ls commands) <(ls .github/prompts | sed 's/\.prompt\.md$/.md/')` empty and every `cmp .github/prompts/<n>.prompt.md commands/<n>.md` silent.
- [x] 2.3 Route the remaining artifact writers to the companion: the Reviewer (`delivery-reviewer.agent.md`, `review-feature.prompt.md`, `review-issue.prompt.md`) writes and commits `review.md` + roadmap repairs in the companion half, pushes it, and posts one verdict comment on the code PR linking the review (`gh pr comment --edit-last` on re-review — no second thread); the Architect (`initiative-architect.agent.md`, `new-initiative.prompt.md`) writes `brief.md`/`breakdown.md` on `changes/initiative-<slug>` in the companion clone, opens and merges that PR in the companion repository; `triage-followups.prompt.md` annotates, branches, and merges in the companion; `delivery-status.prompt.md` shows `companionPr` beside `pr` — verify: `grep -c 'companion' .github/agents/delivery-reviewer.agent.md .github/agents/initiative-architect.agent.md .github/prompts/triage-followups.prompt.md .github/prompts/delivery-status.prompt.md` ≥ 1 each; `grep -c 'edit-last' .github/agents/delivery-reviewer.agent.md` ≥ 1; `node --test tests/customizations.test.mjs` exit 0.
- [x] 2.4 (added 2026-09-16) Drive the mirrored flow end to end on a throwaway /tmp product+companion pair (bare origins, `artifacts.repo.name` set) with the exact commands the prompts now name — Planner: product `feature/x` + empty commit, companion `switch -c feature/x`, roadmap with `artifact-pr`, `git -C` commit + push; Builder: code commit in the product half, roadmap tick in the companion half, both pushed; Reviewer: `review.md` in the companion — and record the transcript as [evidence/step-2-4-pair-drive.txt](evidence/step-2-4-pair-drive.txt) (driven 2026-09-16 with slug `xw` — the CLI requires slugs of ≥ 2 characters; gh calls simulated) — verify: from both halves `agento.mjs session` reports the same `delivery.slug`/`delivery.branch`, `delivery.status`, `delivery.artifactPr: "#7"`, `companion.branch` = `feature/x`, `companion.ahead` = 0, `companion.dirty` = false; from the product primary `agento.mjs resolve feature x` is `source: remote` and `next x` names the candidate with `artifactPr: "#7"` (repaired 2026-09-16: `status` walks only the clone's working tree, so a branch-only roadmap is visible to `resolve`/`next`, not `status`); `close-decision feature x` reports no companion gaps.
- [x] 2.5 (added 2026-09-17) Fix the primary-window companion trigger and the `gh --repo` value (review round 1, Findings 1–2): `initiative-architect.agent.md`, `new-initiative.prompt.md`, `triage-followups.prompt.md`, and `delivery-status.prompt.md` step 3 must detect companion mode the way `start-session`/`close-session`/`ship` do — `agento.mjs config` (`config.artifacts.repo.name`/`dir` non-null, `artifactsRoot`) or `doctor`'s `artifact-repo` check — not the session record's `companion`, which is `null` from the primary (`pairFor` in `scripts/session-state.mjs`); address the clone as `artifactsRoot`, not `companion.path`; and replace every `--repo <artifacts.repo.name>` (Architect, triage, status, new-initiative, Planner step 8.3, `wait-for-checks.sh`) with either `gh` run from inside the companion clone or `gh repo view --json nameWithOwner -q .nameWithOwner` derived there, since `artifacts.repo.name` is a directory basename (`scripts/agento-config.mjs`) — verify: `grep -c 'companion. is not .null' .github/agents/initiative-architect.agent.md .github/prompts/new-initiative.prompt.md .github/prompts/triage-followups.prompt.md` = 0 each; `grep -rc -- '--repo <artifacts.repo.name>' .github/agents .github/prompts | grep -v ':0$'` empty; every `cmp .github/prompts/<n>.prompt.md commands/<n>.md` silent; `node --test tests/customizations.test.mjs` exit 0; on a /tmp product+companion pair from the product primary, `node scripts/agento.mjs session` shows `companion: null` while `node scripts/agento.mjs config` shows `artifactsRoot` ≠ `root` (the condition the prose now names). (done 2026-09-17: Architect, new-initiative, triage-followups, delivery-status step 3, Planner step 8.3, and docs/commands.md now name the `config` trigger, address the clone as `artifactsRoot`, and run `gh` from inside the clone or with `--repo <companion-repo>` derived via `gh repo view --json nameWithOwner`; `wait-for-checks.sh` already documented `--repo OWNER/NAME`; local-only /tmp pair from the product primary: `session` `companion: null`, `config` `root: …/prod`, `artifactsRoot: …/prod-docs`; both greps 0/empty, mirrors `cmp` silent, customizations 19/19/0.)

## Phase 3: Ship integration and verification

- [x] 3.1 ~Add the companion-side merge gate to `/agento ship` and make the merge order explicit: code PR first, companion PR second — verify: the ship preflight reports both `product` and `companion` PR owners and the roadmap step remains aligned.~ (obsolete: the companion merge gate, `ship-preflight { product, companion }`, and the code-then-companion merge order are the `ship-dual-merge` member's scope per the breakdown; this feature only produces the `artifact-pr` header ship-dual-merge consumes. Until it lands, `/agento ship` in companion mode merges the code PR and leaves the companion PR open, which docs/commands.md and the CHANGELOG state.)
- [x] 3.2 Update `docs/commands.md` (`session --pr` → `companionPr`, `artifactPr`, the two-PR flow, the interim ship limitation), `docs/artifacts.md` (`artifact-pr` header, artifacts live on the mirrored branch), `docs/concurrency.md` (integrate both defaults), and `CHANGELOG.md` `## Unreleased`; then run the full-repository gate against the plan's green baseline (gate run 2026-09-16 vs baseline 185/185/0: `node --test` exit 0 `# tests 188`, `# pass 188`, `# fail 0` — the +3 are this feature's new CLI tests; shellcheck exit 0 silent; `replay-guard.sh` exit 0; `REPLAY_COMPANION=1 replay-guard.sh` exit 0 — no new or pre-existing findings) — verify: `grep -c 'artifact-pr\|companionPr' docs/commands.md docs/artifacts.md CHANGELOG.md` ≥ 1 each; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` exit 0 with `# fail 0`; `shellcheck scripts/hooks/delivery-guard.sh scripts/hooks/replay-guard.sh scripts/hooks/session-context.sh scripts/wait-for-checks.sh` exit 0 silent; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0; `REPLAY_COMPANION=1 ./scripts/hooks/replay-guard.sh < tests/guard-fixtures-companion.txt` exit 0.
- [x] 3.3 Promote the feature to review and confirm the initiative member reports `state: in-review` with `errors: []` — verify: `git merge-base --is-ancestor origin/main HEAD` exit 0; `node scripts/agento.mjs initiative external-artifact-repo` lists `mirrored-artifact-branches` as `in-review` with `errors: []`.

## Follow-ups

- `ship-dual-merge` (initiative member): `ship-preflight` gains `{ product, companion }` PR blocks read from `artifact-pr`; `/agento ship` merges the code PR first, then the companion PR, and its audit rejects a companion PR that is missing, closed, or not mergeable. Until then `/agento ship` in companion mode merges only the code PR and leaves the companion PR open (stated in docs/commands.md and the CHANGELOG).
- `ship-dual-merge`: `close-decision`/`ship-preflight` should also report a companion branch whose `origin/<branch>` is behind the half (not just dirty/ahead) so a stale mirrored branch is caught before teardown.
- `artifact-history-migration` (initiative member): move this repository's own in-repo `features/`, `issues/`, and `initiatives/` history into the companion repository and switch `.github/agento.json` to `artifacts.repo`; the `status` subcommand still walks only the companion clone's working tree, so a roadmap that exists only on a mirrored branch is visible to `resolve`/`find`/`next`/`session` but not to `status` (found in 2.4) — decide there whether `status` should walk registered halves too.
- `session --pr` runs `gh --version` once per lookup (product and companion); a shared probe would save one process spawn per call.
