```yaml
status: complete
branch: feature/ship-audit-first
last-updated: 2026-09-14
next-step: ""
initiative: "workflow-orchestration"
```

## Phase 1: Gate and baseline

- [x] 1.1 Integrate `origin/main` (merge, never rebase) and confirm `window-aware-commands` has landed: `sha=$(gh pr view 19 --json mergeCommit --jq .mergeCommit.oid)` is non-empty and `git merge-base --is-ancestor "$sha" HEAD` exits 0; if PR #19 is not merged, run the pause protocol with `status: paused`, `next-step: 1.1` and stop — verify: `git merge-base --is-ancestor "$sha" HEAD; echo $?` prints `0`, and `node scripts/agento.mjs ship-preflight feature ship-audit-first` output contains an `"owner"` key
- [x] 1.2 Re-run the full baseline on the integrated branch and record it in plan.md `## Research` under the existing table (exit statuses, test count) — verify: `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` exits 0; `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh` exits 0; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exits 0

## Phase 2: Tests first

- [x] 2.1 Add `evaluateShipPreflight` ownership tests to `scripts/delivery-roadmap-resolver.test.mjs`: managed worktree on `feature/widget` inside `config.worktrees.dir` → `owner.path` equals that entry and `owner.role === "build"`; promoted `plan-<id>` worktree on the branch → `owner.dirPrefix === "plan"`; call without `worktreeList` → `owner === null`; `status` is `ok` in all three — verify: `node --test scripts/delivery-roadmap-resolver.test.mjs` exits 0 with the three new tests listed as passing
- [x] 2.2 Add the guard test "guidance never sequences close-session before ship (ship-audit-first)" to `tests/customizations.test.mjs`: over `guidanceFiles`, split into blank-line paragraphs and reject `/\/agento close-session\b[^\n]*?(→|\bthen\b|and then)[^\n]*?\/agento ship\b/` and `/\/agento ship\b[^\n]*?\bafter\b[^\n]*?\/agento close-session\b/`; allowlist `CHANGELOG.md`; add canary `paused at teardown` (present in the ship prompt only) to the single-source test — verify: `node --test tests/customizations.test.mjs` FAILS on the current prose (at least `delivery-policy.instructions.md`, `commands/next-feature.md`, `docs/commands.md`, `delivery-reviewer.agent.md` reported), recorded in the commit message
- [x] 2.3 Update `scripts/session-state.test.mjs` for the new §8-as-data order: `primary × approved` `allowed` is `[ship, close-session, delivery-status]`; `build × approved` and `plan × approved` `elsewhere` name ship first with a reason containing `tears`; `build × shipped` `elsewhere` names ship first (resume at teardown) then close-session — verify: `node --test scripts/session-state.test.mjs` FAILS on the current table (order assertions), recorded in the commit message

## Phase 3: Code and policy

- [x] 3.1 Rewrite the `TABLE`, `SHIP_LATER`, `SHIP_NOW`, and header comment in `scripts/session-state.mjs` per plan.md Approach §5 (ship first for approved; ship-then-close for shipped-with-worktree; reasons mention audit-in-place and teardown) — verify: `node --test scripts/session-state.test.mjs scripts/agento.test.mjs tests/session-context.test.mjs` exits 0
- [x] 3.2 Update `.github/instructions/delivery-policy.instructions.md`: §8 item 2 → `/agento ship <slug>` after approval (audits while the worktree is open, sends you back on a rejected audit, tears down after the merge; standalone `/agento close-session` for plan/freehand sessions and abandoned builds); §9 ship row gains "`status: complete`, PR merged, and a managed worktree still owning the branch resumes at teardown"; §10 drops the "`/agento ship` while it still depends on the worktree being gone" parenthetical and lists ship's teardown as a permitted worktree mutation — verify: `grep -n "resumes at teardown" .github/instructions/delivery-policy.instructions.md` matches the ship row; `grep -c "close-session <type>/<slug>\`, then" .github/instructions/delivery-policy.instructions.md` prints `0`
- [x] 3.3 Rewrite `.github/prompts/ship.prompt.md` per plan.md Approach §2 — ownership branch (`owner !== null` read-only audit against `origin/<branch>`, clean + zero-ahead owner, writes via `git -C <owner.path>`, `merge --abort` + reject on conflict; `owner === null` today's primary checkout; never check out in the primary while an owner exists, never a temporary detached checkout), pinned hard-reject and confirmation gap lists with the review-only → `/agento review-<type> <slug>` else `/agento build-<type> <slug>` rule and `gh pr edit` for a missing `Fixes #<n>`, teardown after the `main` sync (`git worktree remove <owner.path>`, `git worktree prune`, `git branch -d <branch>`), the exact pause result line, the §9 teardown-resume sentence, and the updated closing "Never…" paragraph; copy byte-for-byte to `commands/ship.md` — verify: `cmp .github/prompts/ship.prompt.md commands/ship.md` exits 0; `grep -c "paused at teardown (worktree <path> still open); next: close that VS Code window, then /agento ship <slug>" commands/ship.md` prints `1`; `grep -n "close-session" commands/ship.md` shows no "then rerun" precondition
- [x] 3.4 Update `.github/prompts/close-session.prompt.md` intro (ship performs the build close after merging; this command is the normal close for plan/freehand sessions and abandoned builds; closing before ship stays valid) and mirror to `commands/close-session.md` — verify: `cmp .github/prompts/close-session.prompt.md commands/close-session.md` exits 0; `grep -n "abandon" commands/close-session.md` matches the intro

## Phase 4: Handoff prose, docs, and rehearsal

- [x] 4.1 Update handoff prose: `.github/agents/delivery-reviewer.agent.md` step 8, `delivery-builder.agent.md` completion, `delivery-autopilot.agent.md` step 4 and ground rule, `delivery-planner.agent.md` step 9; `.github/prompts/next-feature.prompt.md` L42 (+ `commands/next-feature.md`); `templates/AGENTS-section.md` command list and `.github/prompts/agento-init.prompt.md` step 4 (+ `commands/agento-init.md`) — verify: `node --test tests/customizations.test.mjs` exits 0 (the 2.2 guard test now passes; prompt/command mirrors identical). Also touched here because the verify demands a green guard test: the two `docs/commands.md` flow blocks, and the `new-feature`/`new-issue` handoff sentence (+ mirrors)
- [x] 4.2 Update docs: `README.md` (merge "### 5. Close the session" into a ship-first "### 5. Ship — primary window" describing audit-in-place, reject-back-to-the-open-window, teardown, and the pause; add a "Closing without shipping" note; command table rows), `docs/commands.md` (standard flow ends at ship with teardown; initiative flow line drops the close-session hop; close-session and ship rows), `docs/architecture.md` mermaid (`SHIP -->|reject| B`, `SHIP -->|merge, sync, teardown, epilogue| DONE`, close-session edge labelled plan/freehand/abandon), `CHANGELOG.md` `## 0.4.0 (unreleased)` entry — verify: `node --test tests/customizations.test.mjs` exits 0; `grep -c '^## .*(unreleased)' CHANGELOG.md` prints `1` (verify repaired 2026-09-14: the original `grep -c "unreleased"` counted three pre-existing prose mentions in historical 0.3.0 entries; the intent is one unreleased heading); `grep -n '"version"' plugin.json package.json` both show `0.3.0`
- [x] 4.3 Rehearse the new ship flow in a scratch clone with a temp `worktrees.dir` and a managed worktree on `feature/widget` (complete roadmap, `Verdict: approve`): happy path (`ship-preflight` → `owner` → audit against `origin/feature/widget` → owner clean + zero-ahead → `git -C <owner.path>` status commit → simulated merge → `git worktree remove` → `git worktree prune` → `git branch -d`), reject path (dirty owner → no writes; forced conflicting `merge` → `merge --abort` leaves `status --porcelain` empty), pause path (a shell holding the worktree cwd → guard `ask` on removal → pause result line, no answer given); record transcripts as [evidence/step-4-3-ship-rehearsal.md](evidence/step-4-3-ship-rehearsal.md), [evidence/step-4-3-ship-reject.md](evidence/step-4-3-ship-reject.md), [evidence/step-4-3-ship-pause.md](evidence/step-4-3-ship-pause.md) (rehearsed 2026-09-14 in a throwaway `mktemp -d` clone, removed afterwards) — verify: the three files exist and each ends with the exact `Result:` line the prompt specifies for that path
- [x] 4.4 Full gate on the integrated branch (merge `origin/main` first): `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'`, `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh`, `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt`; compare with the plan.md baseline (no new findings) and record the comparison in plan.md `## Research` — verify: all three exit 0 and plan.md `## Research` records the final run
