```yaml
status: complete
branch: feature/window-aware-commands
last-updated: 2026-09-14
next-step: ""
initiative: "workflow-orchestration"
```

## Phase 1: Session record — hosted flag, worktrees[], owner lookup

- [x] 1.1 Extend `scripts/session-state.mjs`: `deriveRole` accepts optional `env` and returns `hosted` (true when `env.CODESPACES === "true"` or `env.GITHUB_ACTIONS === "true"`; role then derived from the branch prefix only — `build` on a `feature/`/`issue/` branch, else `primary` — with a `reason` string for `warnings[]`); add exports `classifyWorktrees({ worktrees, worktreesDir, config })` (one `{ path, branch, detached, role, dirPrefix, id, isPrimary, isManaged }` per registered entry via `deriveRole` with `cwd: entry.path`) and `findOwner({ worktrees, worktreesDir, branch, config })` (entry whose branch equals `branch`: managed inside the realpath of `worktreesDir` → `{ path, role, dirPrefix, id }`; primary → `{ …, role: "primary" }`; else `null`) — verify: `node --test scripts/session-state.test.mjs` passes new tests for `CODESPACES=true` on `feature/x` → `build`, `GITHUB_ACTIONS=true` on `main` → `primary`, hosted ignoring the `worktrees.dir` path, `env` absent → results unchanged, `classifyWorktrees` over primary + `plan-` + promoted `plan-` + `feature-` + `freehand-` + stray sibling, and `findOwner` managed / primary / none
- [x] 1.2 Wire the record in `scripts/agento.mjs` `session`: pass `process.env` to `deriveRole`, emit `hosted` and `worktrees` (classified), append the hosted reason to `warnings`, update the usage header line — verify: `node scripts/agento.mjs session` in this worktree prints `hosted: false` and a `worktrees[]` entry for `plan-20260914-223201` with `role: "build"`; `node --test scripts/agento.test.mjs` passes new tests for `worktrees[]` contents and `runWith({ env: { GITHUB_ACTIONS: "true" } })` → `hosted: true` + one warning; `node scripts/agento.mjs` usage mentions `worktrees`
- [x] 1.3 Replace the heuristic in `scripts/delivery-roadmap-resolver.mjs` `closeBuildSessionDecision`: parse `worktreeList` with `parseWorktreeList`, use `findOwner` for `result.branch`, return `owner` and reasons `managed-worktree-present` | `primary-owns-branch` (new; message says return the primary to the default branch first) | `remote-roadmap-only`; delete the basename regex and the `currentBranch !== default` fallback; give `evaluateShipPreflight` an optional `worktreeList` and the same `owner` field (`null` without a list); pass `git worktree list --porcelain` from the CLI's `ship-preflight` case — verify: `node --test scripts/delivery-roadmap-resolver.test.mjs` passes with the L186–206 fixture rewritten to `path.resolve(root, config.worktrees.dir, "feature-widget")` (absolute form of `path.join(config.worktrees.dir, …)`, first entry = `root`) and new cases: primary on `feature/widget` → `primary-owns-branch`; lookalike path outside `worktrees.dir` → `remote-roadmap-only`, `owner: null`; `currentBranch: "feature/widget"` with no owning entry → `remote-roadmap-only`; `evaluateShipPreflight` with/without `worktreeList`; `grep -n "currentBranch !== " scripts/delivery-roadmap-resolver.mjs` empty; `node --test scripts/agento.test.mjs` still passes L140
- [x] 1.4 Integrate `origin/main` by merge, run the full gate, push — verify: `git merge origin/main` clean (if `capability-preflight` has landed, note here that the policy section number for Phase 2 becomes 11); `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh` exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures; `bash scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0 — done 2026-09-14: `origin/main` still at `3d2bac3` (already an ancestor, `capability-preflight` not landed, section stays §10); shellcheck exit 0; tests 110 pass / 0 fail; replay-guard exit 0

## Phase 2: Policy §10 and enforcement tests

- [x] 2.1 Add `## 10. Window check` to `.github/instructions/delivery-policy.instructions.md` (next free number if `origin/main` already has a §10 — record the number on this line: **§10**, `origin/main` had none) with the five-point procedure from plan.md Approach §3 (run `agento.mjs session`; compare `role` to the command's `requires role` line; mismatch → the §9 `rejected` form with the record's alternatives; `unmanaged` always rejects and names the primary checkout from `worktrees[0].path`; `hosted` needs no special handling; branch conditions read `worktree.branch`) and no roles table; extend the frontmatter `description` — verify: the section contains no `allowed:` table; `node --test tests/customizations.test.mjs` passes (canaries, §N exists)
- [x] 2.2 Extend `tests/customizations.test.mjs`: bump `sections.size >= 9` to `>= 10` (or the new count); add "every command and agent declares its window check (§10)" asserting `/Window check per .*§10.*requires role/` over `promptFiles` + `agentFiles`; add "only worktree-mutating commands inspect `git worktree list --porcelain`" over prompts, `commands/`, and agents with the allowlist `start-session`, `start-freehand`, `close-session`, `ship` (comment: `ship-audit-first` removes `ship`) — verify: `node --test tests/customizations.test.mjs` now FAILS on exactly the two new tests (every file lacks the line; non-allowlisted files still contain the literal), proving the assertions bite; record the failing file counts on this line — done 2026-09-14: 12 pass / 2 fail (only the two new tests); 28 prompt+agent files lack the §10 line; 4 non-allowlisted files match `worktree list\s+--porcelain` (finish-freehand prompt + mirror, Planner, Architect — the literal match is whitespace-tolerant because the Planner wraps it)

## Phase 3: Consumers — prompts, mirrors, agents

- [x] 3.1 Primary-window prompts: `start-session` (shared precondition 1 → §10 line `requires role primary`; build mode step 2 keeps the ownership scan but decides from `worktrees[]`: primary owner → stop; managed owner → resume), `start-freehand`, `quick-fix`, `new-initiative` (primary on the default branch, clean), and the Architect agent (L41–43 → §10 line) — verify: `grep -c "worktree list --porcelain" .github/prompts/{quick-fix,new-initiative}.prompt.md .github/agents/initiative-architect.agent.md` all 0; each file has the §10 line; `for f in start-session start-freehand quick-fix new-initiative; do diff commands/$f.md .github/prompts/$f.prompt.md; done` empty
- [x] 3.2 `close-session`: shared rule 1 → §10 `requires role primary`; build close reads `owner` — `managed-worktree-present` → continue, `primary-owns-branch` → stop with "return the primary to `main` first, nothing to remove", `remote-roadmap-only` → already closed; shared rule 6 keeps `git worktree list --porcelain` for the registration check — verify: `grep -n "primary-owns-branch\|owner" .github/prompts/close-session.prompt.md` shows the three reasons; `diff commands/close-session.md .github/prompts/close-session.prompt.md` empty
- [x] 3.3 `ship`: §10 `requires role primary`; rewrite only the L25–31 precondition paragraph to read `ship-preflight.owner` (managed → stop with `/agento close-session <type>/<slug>`; `primary-owns-branch` → stop, return the primary to `main`; `null` → proceed); leave the rest untouched for `ship-audit-first` — verify: `git diff origin/main -- .github/prompts/ship.prompt.md` touches only the receipt paragraph and the precondition paragraph; `diff commands/ship.md .github/prompts/ship.prompt.md` empty
- [x] 3.4 Build-window prompts and agents: `build-feature`, `build-issue`, `review-feature`, `review-issue`, `ap` (§10 `requires role build`, delivery slug must equal the argument) and the Builder, Reviewer, Autopilot agents ("if another worktree owns the branch" → compare `delivery.slug` / `worktree.branch` and read the other owner from `worktrees[]`; the rejection's alternatives come from the record) — verify: each of the 8 files has the §10 line and none contains `worktree list --porcelain`; `for f in build-feature build-issue review-feature review-issue ap; do diff commands/$f.md .github/prompts/$f.prompt.md; done` empty
- [x] 3.5 Planning prompts and agent: `new-feature`, `new-issue` (§10 `requires role plan` — or `build` when resuming this slug's promoted planning worktree) and the Planner (step 1 isolation → §10 + `worktree.isManaged && dirPrefix === "plan"` from the record; delete the "GitHub-hosted … exempt" sentence in favour of "hosted workspaces are handled by the record's `hosted` flag"; L98 and L136–138 reworded to the record's vocabulary) — verify: `grep -n "coding-agent workspace is exempt" .github/agents/delivery-planner.agent.md` empty; `grep -n "hosted" .github/agents/delivery-planner.agent.md` non-empty; `for f in new-feature new-issue; do diff commands/$f.md .github/prompts/$f.prompt.md; done` empty
- [x] 3.6 `finish-freehand` (§10 `requires role freehand`, delivery slug = argument; L18–23 porcelain check removed) and `commit-current-changes` (§10 `requires role primary on a non-default branch`; plan/build/freehand rejected with the record's alternatives — no hand-maintained list) — verify: `grep -c "worktree list --porcelain" .github/prompts/{finish-freehand,commit-current-changes}.prompt.md` both 0; `grep -n "non-default" .github/prompts/commit-current-changes.prompt.md` shows the rule; both mirrors `diff` empty
- [x] 3.7 Read-only / non-window-sensitive prompts and the Mechanic agent: `delivery-status`, `next-feature`, `agento-init`, `install-skills`, `triage-followups`, `extend-copilot`, `fix-copilot` gain `Window check per §10: requires role any (read-only / not window-sensitive)` — verify: `node --test tests/customizations.test.mjs` passes all tests including the two from 2.2; `grep -L "§10" .github/prompts/*.prompt.md commands/*.md .github/agents/*.agent.md` empty; `grep -l "worktree list --porcelain" .github/prompts/*.prompt.md commands/*.md .github/agents/*.agent.md` lists exactly `start-session`, `start-freehand`, `close-session`, `ship` (prompt + mirror each) — done 2026-09-14: 14 pass / 0 fail; repair: the §10 citation regex is single-line, so the phrase in `quick-fix`, `ship` (from 3.1/3.3) and the Autopilot (3.4) was reflowed onto one line

## Phase 4: Docs, changelog, final gate

- [x] 4.1 Update `docs/commands.md` (CLI paragraph: `session` gains `hosted`, `worktrees[]`; `close-decision`/`ship-preflight` gain `owner` and the `primary-owns-branch` reason), `docs/architecture.md` (policy summary adds the window check §10), `docs/hooks.md` and `README.md` (`hosted` flag on the record), and `CHANGELOG.md` under `## 0.4.0 (unreleased)` with a **Window-aware commands (policy §10)** entry; no version bump — verify: `grep -n "hosted\|owner\|Window check\|§10" docs/commands.md docs/architecture.md docs/hooks.md README.md CHANGELOG.md` shows each file; `git diff origin/main -- plugin.json package.json` empty; `node --test tests/customizations.test.mjs` passes
- [x] 4.2 Final gate against the baseline in plan.md `## Research` (101 pass / 0 fail; shellcheck 0; replay 0), integrate `origin/main` by merge (keep both CHANGELOG entries and renumber §10 → §11 if `capability-preflight` landed), push, set `status: in-review` — verify: `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh` exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures and total > 101; `bash scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0; `git diff --stat origin/main` touches only the files in plan.md Approach "Files touched"; `node scripts/agento.mjs session` here reports `lifecycle: "in-review"` — done 2026-09-14: shellcheck exit 0; tests 112 pass / 0 fail (baseline 101/0, no pre-existing findings to carry); replay-guard exit 0; `origin/main` still at `3d2bac3` and already an ancestor (`capability-preflight` not landed, section stays §10, single CHANGELOG entry); diff vs `origin/main` touches 65 files, all within "Files touched" (no `scripts/hooks/*`, `AGENTS.md`, `plugin.json`, `package.json`)
- [x] 4.3 (added 2026-09-14) Integrate `origin/main` `42d111c` (`capability-preflight` #18 merged during review; it owns `## 10. Capability preflight`): `git merge origin/main` (never rebase; conflicts expected in the policy file, 17 prompts + 17 `commands/` mirrors, `docs/architecture.md`, `docs/commands.md`, `scripts/agento.mjs`, `scripts/agento.test.mjs`, `tests/customizations.test.mjs`), renumber `## 10. Window check` → `## 11. Window check` and every `§10` citation of this delivery → `§11` (policy frontmatter `description`, all prompts/mirrors/agents `Window check per §11` lines, `docs/*`, `README.md`, the CHANGELOG entry title, the `customizations.test.mjs` window-check regex and `sections.size >= 11`), keep `capability-preflight`'s `Needs:`/`Fallback:` lines, its `§10` citations, and both CHANGELOG entries — verify: `git merge-base --is-ancestor origin/main HEAD` exit 0; `grep -n "^## " .github/instructions/delivery-policy.instructions.md` shows `## 10. Capability preflight` and `## 11. Window check`; `grep -L "§11" .github/prompts/*.prompt.md commands/*.md .github/agents/*.agent.md` empty; `for f in .github/prompts/*.prompt.md; do cmp -s "$f" "commands/$(basename "$f" .prompt.md).md" || echo "$f"; done` empty; `shellcheck scripts/hooks/*.sh scripts/wait-for-checks.sh` exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures vs baseline 101/0; `bash scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0; `git diff origin/main...HEAD -- scripts/hooks .github/hooks plugin.json package.json` empty; push — done 2026-09-14: merge commit `bb5a4da` resolved 40 conflicts (policy, 17 prompts + 17 mirrors, `docs/architecture.md`, `docs/commands.md`, `agento.mjs`, `agento.test.mjs`, `customizations.test.mjs`) keeping both sides; new `doctor` prompt + mirror gained the `§11` line; `merge-base --is-ancestor` exit 0; policy headings `## 10. Capability preflight` / `## 11. Window check`; `grep -L "§11"` empty; mirror `cmp` loop empty (23 files); shellcheck exit 0; tests 120 pass / 0 fail (baseline 101/0); replay-guard exit 0; hooks/plugin/package diff empty; plan.md `## Approach` and `## Acceptance checklist` carry a one-line §11 note

## Follow-ups

- The `FREEHAND` row of `deriveAllowed` (`scripts/session-state.mjs`) still lists `/agento commit-current-changes` as allowed in a freehand worktree, while the command's §10 line now requires role `primary` on a non-default branch (Decision Q1). Changing table rows is out of scope here; a follow-up should drop it from the `FREEHAND` row so the record's alternatives match the command's requirement. → filed as #33
