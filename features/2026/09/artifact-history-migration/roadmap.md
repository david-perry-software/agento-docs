```yaml
status: in-progress
branch: feature/artifact-history-migration
last-updated: 2026-09-18
next-step: "6.1 prompt smoke on a throwaway pair"
initiative: "external-artifact-repo"
artifact-pr: "#1"
```

## Phase 1: CLI — layout rule and branch-aware resolution

- [x] 1.1 In `scripts/agento.mjs` `resolveArtifacts()`, apply the layout rule from plan.md `## Decisions` ("the checkout decides, the primary anchors"): when the current checkout's `artifacts.repo` is set, resolve with the primary's config only when it also sets `artifacts.repo`, else with the checkout's own config — both against `primaryRoot`; unset stays in-repo with no git call; update the comment above the function; add a `scripts/agento.test.mjs` test: `makeRepo({ companion: true })` without `artifacts.repo` in the primary, a managed worktree `<wt>/plan-1` (config `worktrees.dir: ../wt`) on `feature/flip` that commits `.github/agento.json` `{ "artifacts": { "repo": { "name": "project-docs" } } }`; assert `config` from the worktree reports `artifactsRoot === companionOf(repo)` (not `<wt>/project-docs`), `session` there reports `companion.registered: false` with `companion.path` under `project-docs-worktrees/plan-1`, and `config`/`session` from the primary stay in-repo (`artifactsRoot === repo`, `companion: null`); the existing test "a managed worktree resolves the companion beside the primary checkout" still passes — verify: `node --test scripts/agento.test.mjs` exit 0 with the new test listed; `node scripts/agento.mjs config` in this worktree still reports `artifactsRoot` equal to `root`.
- [x] 1.2 Export `parseConfigText(text, rootDir)` from `scripts/agento-config.mjs` (JSON parse + the existing `mergeConfig` over `defaultConfig(rootDir)`; `loadAgentoConfig` reuses it) and add `layoutFor(branch)` to `scripts/agento.mjs` per plan.md `## Approach` item 2: `artifacts.external` → the module-level objects with `layout: "checkout"`; else `git show origin/<branch>:.github/agento.json` then `<branch>:.github/agento.json`, `resolveArtifactsRoot({ config: branchConfig, rootDir: root, primaryRoot })`, and when `external` and `git -C <dir> rev-parse --show-toplevel` is `<dir>` return `{ artifacts, artifactsRoot: dir, artifactsGit, companionWorktreesDir, companionWorktrees, layout: "branch" }`, else `null` (an absent companion is reported by the caller's `missing` message naming `<dir>` and `/agento agento-init`); add `scripts/agento-config.test.mjs` cases for `parseConfigText` (nulls keep defaults; nested `repo`) — verify: `node --test scripts/agento-config.test.mjs` exit 0; `node scripts/agento.mjs config` unchanged in this worktree.
- [x] 1.3 Route `resolve`, `find`, `close-decision`, `ship-preflight`, and `roadmapOnBranch()` (used by `next`) through the fallback: on `status: "missing"` in an in-repo checkout, retry with `layoutFor(<prefix><slug>)` when non-null; parametrise `companionOfOwner`, `companionGaps`, `lookupCompanionPullRequest`, and the `--pr` gap logic on the layout's companion objects; emit `layout` and `artifactsRoot` on `resolve`/`find`/`close-decision`/`ship-preflight` results and on `next` candidates; add tests per plan.md `## Approach` item 2 (product `feature/flip` commits only the config; companion `feature/flip` pushed with `features/2026/09/flip/roadmap.md` `status: in-review`, `artifact-pr: "#7"`; from the primary on `main`: `resolve`/`find` → `ok`, `source: remote`, `layout: branch`, `artifactsRoot === companionOf(repo)`; `ship-preflight --pr` with `prStub` → `companionPr.number === 7`, `companionGaps: []`, second `gh` call's `$PWD` in `project-docs`; `close-decision` → `remote-roadmap-only`; `next flip` → `status: ok`; config naming an absent companion → `missing` with the path in `message`; a plain in-repo branch → today's fields plus `layout: "checkout"`) — verify: `node --test scripts/agento.test.mjs` exit 0 with the new tests listed; `node scripts/agento.mjs find artifact-history-migration` in this worktree prints `"layout": "checkout"` and `"source": "local"`.
- [x] 1.4 Make `paths <feature|issue> <slug>` branch-aware: when not external, consult `layoutFor(branch)` and, when non-null, fill `artifactsRoot`, `artifactRoot`, `companion`, and `workspace` from it (plan/freehand kinds unchanged); extend the existing `paths` tests with the `feature/flip` fixture (companion/workspace non-null from the in-repo primary) and an in-repo control (`companion: null`) — verify: `node --test scripts/agento.test.mjs` exit 0; `node scripts/agento.mjs paths feature artifact-history-migration` in this worktree still prints `"companion": null`.

## Phase 2: CLI — `migrate` subcommand

- [x] 2.1 Add `case "migrate"` to `scripts/agento.mjs` per plan.md `## Decisions` (`migrate <destination> [--apply]`; `parseArgs` learns `--apply`; usage header line added): validate the destination (git toplevel; clone = first `git -C <dest> worktree list --porcelain` entry; clone must be a sibling of the primary root → else `status: "error"`, `reason: "not-sibling"`, exit 3); source roots = `config.artifacts.{features,issues,initiatives}` under `root`; dry run emits `mode: "dry-run"`, `source`, `destination`, `clone`, `name`, `roots[] { rel, files, bytes }`, `conflicts[]`, `records { roadmaps[], breakdowns[] }`; conflicts (any non-`.gitkeep` file the copy would overwrite) → `status: "conflict"`, exit 3, nothing written; no artifacts under any source root → `mode: "nothing-to-migrate"`, `configSet`, exit 0 — verify: `node scripts/agento.mjs migrate /tmp` exits 3 with `reason: "not-sibling"` (or a toplevel error) and writes nothing; `node scripts/agento.mjs migrate /home/david/DP/agento-worktrees` exits 3; the usage output lists `migrate <companion-checkout> [--apply]`.
- [x] 2.2 Implement `--apply`: `fs.cpSync(src, dest, { recursive: true })` per root, `fs.rmSync` the source roots, write `.github/agento.json` (create from `templates/agento.json` with `artifacts.repo.name = name` when absent; otherwise parse, set only `artifacts.repo.name`, write back with 2-space JSON), append the `## Migrated history` section to `<dest>/README.md` when the heading is absent (wording from plan.md `## Decisions`, `<owner>/<repo>` from `git remote get-url origin` of the product when parseable, else the repo basename), re-describe the destination roots and emit `records.identical` and `records.diff[]`, `moved[]`, `configWritten`, `readmeNoteAdded`, `mode: "applied"` — verify: `node --test scripts/agento.test.mjs` exit 0 (tests land in 2.3); `node scripts/agento.mjs migrate <a fresh /tmp git repo that is a sibling of nothing>` still exits 3 without writing.
- [x] 2.3 Add `scripts/agento.test.mjs` tests for `migrate` using `makeRepo({ companion: true })`: dry run writes nothing (`git status --porcelain` empty in both repos) and lists two roots' files and the records; a destination conflict (`features/2026/09/x/roadmap.md` pre-existing in the clone) exits 3 with `conflicts` naming it and writes nothing; `--apply` into the clone moves a tree containing a binary `evidence/step-1-1-x.png` (byte-equal, `Buffer.compare`), removes the source roots, creates `.github/agento.json` with `"name": "project-docs"` from the template, preserves `branches.default: "trunk"` when a config exists, appends the README note once, reports `records.identical: true`; after `git add -A && git commit` in both repos, `status` and `initiative <slug>` from the product list the same slugs as the dry-run `records`; a second `--apply` reports `nothing-to-migrate` with the README byte-unchanged; `--apply` into a registered half `<base>/project-docs-worktrees/plan-1` resolves `name: "project-docs"` from the clone; a non-sibling destination → `not-sibling` — verify: `node --test scripts/agento.test.mjs` exit 0 with every new `migrate` test listed as passing.

## Phase 3: Hooks — layout rule (approval-gated edits)

- [x] 3.1 In `scripts/hooks/delivery-guard.sh` `resolve_artifacts()`: when the primary differs from the product root and the primary's `artifacts.repo` is unset, keep the product root's own `repo` instead of returning in-repo (one edit; comment updated to state the rule); add `tests/guard.test.mjs` tests with `makeGitRepo({ companion: true })`: a managed worktree on `feature/widget` (config `worktrees.dir`) whose branch commits the companion config while the primary has none → `git -C <companion> push origin main` from that worktree is `deny` naming `main`, and `git commit -m x` there with only `code.js` staged is `ask` with a reason matching `/companion|project-docs/` — verify: `shellcheck scripts/hooks/delivery-guard.sh` silent (no output redirection); `node --test tests/guard.test.mjs` exit 0 with the new tests listed; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0; `REPLAY_COMPANION=1 ./scripts/hooks/replay-guard.sh < tests/guard-fixtures-companion.txt` exit 0.
- [x] 3.2 In `scripts/hooks/session-context.sh` apply the identical helper change (keep both copies byte-identical between the `shared with` markers); add `tests/session-context.test.mjs` test: the same worktree setup prints `Artifacts: <companion> (branch main)` directly after `Session:` and lists a companion roadmap while ignoring a product one; the no-node fallback equals with-node minus `Session:` — verify: `shellcheck scripts/hooks/session-context.sh` silent; `node --test tests/session-context.test.mjs` exit 0 with the new test listed; `diff <(sed -n '/--- shared with/,/--- end shared helpers/p' scripts/hooks/session-context.sh) <(sed -n '/--- shared with/,/--- end shared helpers/p' scripts/hooks/delivery-guard.sh)` empty (Builder note 2026-09-18: the opening marker line itself names the other file and differs on `main` too; the diff is empty after `sed '1d'` on both sides — bodies byte-identical).

## Phase 4: Prompts, policy, docs, changelog, version

- [x] 4.1 `.github/prompts/agento-init.prompt.md`: frontmatter `description` mentions `--migrate`, `argument-hint: "[--force] [--migrate]"`; the idempotency paragraph gains "with `--migrate`, roots already moved report nothing to migrate and existing `changes/agento-init` PRs in either repository are resumed"; step 10's `warn` sentence points at `--migrate`; new `## Migration (--migrate)` section after step 11 with the M-steps from plan.md `## Decisions` (dry run `node <agento-root>/scripts/agento.mjs migrate ../<name>`; `gh pr list --state open --json headRefName` refusal on delivery-prefix branches unless the user accepts via the ask-questions tool or its declared fallback; `git -C ../<name> switch -c changes/agento-init origin/<default>`; `migrate ../<name> --apply`; `git -C ../<name> add -A` + commit `docs(migration): import delivery artifacts from <owner>/<repo>` + `push -u origin changes/agento-init` + `cd ../<name> && gh pr create --draft …`; product commit `chore(agento-init): move delivery artifacts to <name>` on `changes/agento-init`, push, PR whose body links the companion PR and says to merge it first; `gh pr edit` cross-link; report both PR URLs and the merge order); copy to `commands/agento-init.md` — verify: `grep -c -- '--migrate' .github/prompts/agento-init.prompt.md` ≥ 4; `grep -c 'nothing-to-migrate\|nothing to migrate' .github/prompts/agento-init.prompt.md` ≥ 1; `cmp .github/prompts/agento-init.prompt.md commands/agento-init.md` silent; `node --test tests/customizations.test.mjs` exit 0.
- [x] 4.2 `.github/prompts/ship.prompt.md`: read `artifactsRoot` and `layout` from the `ship-preflight` result (fall back to `agento.mjs config` only when the field is absent) and add `layout: "branch"` to the companion-mode condition; copy to `commands/ship.md` — verify: `grep -c '"branch"' .github/prompts/ship.prompt.md` ≥ 1; `grep -c 'artifactsRoot' .github/prompts/ship.prompt.md` ≥ 4; `cmp .github/prompts/ship.prompt.md commands/ship.md` silent; `node --test tests/customizations.test.mjs` exit 0.
- [x] 4.3 Policy and docs: `.github/instructions/delivery-policy.instructions.md` §9 `/agento agento-init` row gains the `--migrate` clause; `README.md` (set-up table row for init, flow table row `/agento agento-init [--force] [--migrate]`, a "Migrating an existing project" paragraph: one command, merge the companion PR then the product PR, in-flight deliveries must ship first); `docs/install.md` (same paragraph, shorter); `docs/commands.md` (init row; `migrate` subcommand paragraph; `layout`/`artifactsRoot` fields on `resolve`, `find`, `close-decision`, `ship-preflight`, `paths`; the layout rule); `docs/architecture.md` (layout rule paragraph beside "Companion-cwd anchoring"); `docs/artifacts.md` (in-repo layout is the pre-migration state; migrated plans keep historical links); `docs/project-profile.md` (`artifacts.repo` rows: the checkout's own config decides, the primary anchors and wins when set); `AGENTS.md` (layout bullets: this repository's `features/`, `issues/`, `initiatives/` live in the companion `../agento-docs`; `.github/agento.json` names it) — verify: `grep -l -- '--migrate' README.md docs/install.md docs/commands.md .github/instructions/delivery-policy.instructions.md` lists all four; `grep -c 'layout' docs/commands.md docs/architecture.md docs/project-profile.md` ≥ 1 each; `grep -c 'agento-docs' AGENTS.md` ≥ 1; `node --test tests/customizations.test.mjs` exit 0.
- [x] 4.4 Version and changelog: set `"version": "0.5.0"` in `package.json` and `.claude-plugin/plugin.json`; rename `## Unreleased` to `## 0.5.0 (unreleased)` in `CHANGELOG.md` and add the first bullet **Artifact history migration** (the `migrate` subcommand, `/agento agento-init --migrate`, the layout rule and `layout: "branch"` resolution, hooks, this repository's move to `agento-docs`, the version bump for the layout change) — verify: `grep -c '"version": "0.5.0"' package.json .claude-plugin/plugin.json` = 1 each; `grep -c '^## 0.5.0 (unreleased)' CHANGELOG.md` = 1; `grep -c '^## Unreleased' CHANGELOG.md` = 0; `node --test tests/customizations.test.mjs` exit 0.

## Phase 5: Dogfood — migrate this repository onto `agento-docs`

- [x] 5.1 Create or adopt the companion repository per init steps 2–5 with `<name>` `agento-docs` (created 2026-09-18; ruleset id 23676315): `gh repo view david-perry-software/agento-docs` (adopt) or `gh repo create david-perry-software/agento-docs --<visibility of the product> --description "Agento delivery artifacts for david-perry-software/agento"`; clone to `/home/david/DP/agento-docs` (sibling of the primary `/home/david/DP/agento`, never inside the worktrees dir); bootstrap `README.md` (from `templates/companion-README.md` with the marker `<!-- agento-companion: david-perry-software/agento -->`), `features/.gitkeep`, `issues/.gitkeep`, `initiatives/.gitkeep`, `.github/instructions/agento.instructions.md` on `main` through the Contents API (`gh api -X PUT repos/…/contents/<path>`, README first — never `git push` to `main`); then create the ruleset (`pull_request`, `non_fast_forward`, `deletion`, `~DEFAULT_BRANCH`); `git -C /home/david/DP/agento-docs fetch origin && git -C /home/david/DP/agento-docs checkout main` — verify: `git -C /home/david/DP/agento-docs ls-tree -r --name-only origin/main` lists the five bootstrap files; `gh api repos/david-perry-software/agento-docs/rulesets --jq '.[].name'` lists `Agento default branch` (or the 403/422 gap is recorded on this line); `git -C /home/david/DP/agento-docs branch --show-current` prints `main`.
- [x] 5.2 Create this session's companion half and workspace file: `git -C /home/david/DP/agento-docs worktree add --no-track -b feature/artifact-history-migration /home/david/DP/agento-docs-worktrees/plan-20260917-231812 origin/main`; write `/home/david/DP/agento-worktrees/plan-20260917-231812.code-workspace` with `folders: [{ path: /home/david/DP/agento-worktrees/plan-20260917-231812 }, { path: /home/david/DP/agento-docs-worktrees/plan-20260917-231812 }]`, `settings: {}`; add the half to this window (`code --add /home/david/DP/agento-docs-worktrees/plan-20260917-231812`, or open the workspace file) so the edit tools reach it — verify: `git -C /home/david/DP/agento-docs worktree list --porcelain` lists the half on `branch refs/heads/feature/artifact-history-migration`; `test -f /home/david/DP/agento-worktrees/plan-20260917-231812.code-workspace`; `node scripts/agento.mjs paths feature artifact-history-migration` still prints `"companion": null` (the product config is not written yet).
- [x] 5.3 Snapshot before the move: from this worktree write `node scripts/agento.mjs status`, `node scripts/agento.mjs initiative external-artifact-repo`, `node scripts/agento.mjs initiative workflow-orchestration`, `node scripts/agento.mjs session`, and `find features issues initiatives -type f | sort` (with `wc -l` and `evidence` file count) into `features/2026/09/artifact-history-migration/evidence/step-5-4-migrate-before.json` (one JSON object keyed by command; the file moves with the tree) and commit it on the product branch (`docs(feature): snapshot artifacts before migration`) — verify: `git log -1 --name-only` lists the evidence file; `jq '.status.items | length' <file>` = 17 and `jq '.files | length'` equals `find … | wc -l` at that moment (Builder note 2026-09-18: items = 18 — the plan's 17 predates this feature's own directory: 17 features + 1 issue; `files` = 77 at capture, matching `find` before the snapshot file itself existed; 18 evidence files).
- [ ] 5.4 Run the move: `node scripts/agento.mjs migrate /home/david/DP/agento-docs-worktrees/plan-20260917-231812` (dry run — `conflicts: []`, 3 roots), then `… --apply`; save the `--apply` JSON as `evidence/step-5-4-migrate-after.json` **in the companion half** (the slug directory now lives at `/home/david/DP/agento-docs-worktrees/plan-20260917-231812/features/2026/09/artifact-history-migration/`) — verify: the apply output has `mode: "applied"`, `records.identical: true`, `configWritten` ending in `.github/agento.json`, `readmeNoteAdded: true`; `test ! -d features -a ! -d issues -a ! -d initiatives` in this worktree; `jq -r .artifacts.repo.name .github/agento.json` prints `agento-docs`; `git -C /home/david/DP/agento-docs-worktrees/plan-20260917-231812 status --porcelain | grep -c 'roadmap.md'` = 19 (Builder note 2026-09-18: 18 with `-uall` — porcelain collapses untracked directories without it; 17 feature roadmaps + 1 issue roadmap, matching the dry run's `records.roadmaps`; 3 roots, `conflicts: []`, 78 files moved, [evidence](evidence/step-5-4-migrate-after.json)).
- [x] 5.5 Companion side: `git -C <half> add -A && git -C <half> commit -m "docs(migration): import delivery artifacts from david-perry-software/agento"`, `git -C <half> push -u origin feature/artifact-history-migration`; `cd <half> && gh pr create --draft --head feature/artifact-history-migration --base main --title "docs(feature): artifact-history-migration" --body "<link to code PR>"`; write `artifact-pr: "#<m>"` into the roadmap header (in the half) next to `initiative:`, commit `docs(feature): record artifact PR for artifact-history-migration`, push; `gh pr edit <code PR> --body` in the product to append the companion PR URL — verify: `git -C <half> status --porcelain` empty and `git -C <half> rev-list --count @{upstream}..HEAD` = 0; `cd <half> && gh pr view --json number,isDraft` shows the draft PR; `grep -c 'artifact-pr: "#' <half>/features/2026/09/artifact-history-migration/roadmap.md` = 1; the code PR body contains the companion PR URL (Builder note 2026-09-18: companion PR agento-docs#1; `gh pr edit` failed on the projectCards GraphQL deprecation, body patched via `gh api -X PATCH repos/…/pulls/45`).
- [x] 5.6 Product side: `git add -A` (root deletions, `.github/agento.json`) and commit `feat(migration): move delivery artifacts to agento-docs` (the guard's nudge may `ask` because it inspects the companion clone, not the half — answer it; the half's `HEAD` import commit is the roadmap change), integrate `origin/main` by merge, push — verify: `git ls-tree -r --name-only HEAD | grep -c '^features/\|^issues/\|^initiatives/'` = 0; `git ls-tree -r --name-only HEAD .github/agento.json` = 1 line; `git status --porcelain` empty; `git rev-list --count @{upstream}..HEAD` = 0.
- [x] 5.7 Confirm the session converted (read-only): `node scripts/agento.mjs config` → `artifactsRoot: /home/david/DP/agento-docs`; `node scripts/agento.mjs session --pr` → `companion.registered: true`, `companion.branch: feature/artifact-history-migration`, `companion.dirty: false`, `companion.ahead: 0`, `delivery.roadmap: features/2026/09/artifact-history-migration/roadmap.md`, `delivery.artifactPr` = `companionPr.number`, `lifecycle: building`; `node scripts/agento.mjs doctor --for build-feature` exit 0 (`artifact-repo` `warn` naming stale roots in the primary is expected until merge); from the primary: `node scripts/agento.mjs ship-preflight feature artifact-history-migration --pr --root /home/david/DP/agento` → `layout: "branch"`, `artifactsRoot: /home/david/DP/agento-docs`, `companionPr` non-null, `companionGaps: []`; `node scripts/agento.mjs initiative external-artifact-repo` → member `in-progress`, `errors: []`; record the four outputs (trimmed) in `evidence/step-5-7-session-converted.md` in the half, commit (`docs(feature): record converted session`), push — verify: every listed field matches; the evidence file is on `origin/feature/artifact-history-migration` in the companion ([evidence](evidence/step-5-7-session-converted.md); Builder note 2026-09-18: the `ship-preflight` check must run this branch's CLI with `--root /home/david/DP/agento` — `main`'s own pre-migration CLI has no `layoutFor`; `initiative` matches only with `--root <half>`, since from the build worktree `artifactsRoot` is the companion clone on `main`, empty until agento-docs#1 merges — follow-up below).

## Phase 6: Prompt smoke, gate, in-review, post-ship

- [ ] 6.1 Smoke the rewritten prompt on a throwaway pair: `gh repo create david-perry-software/agento-smoke-migrate-<YYYYMMDD> --public --clone` in a temp dir, seed `features/2026/09/demo/{plan.md,roadmap.md,evidence/step-1-1-demo.png}`, `issues/2026/09/bug/roadmap.md`, `initiatives/2026/09/init/breakdown.md` (valid headers), commit and push `main`; then drive `/agento agento-init --migrate`'s steps literally from that primary checkout (companion name `agento-smoke-migrate-<YYYYMMDD>-docs`); second pass: rerun the steps — verify: `git -C <tmp>/<name>-docs ls-tree -r --name-only origin/changes/agento-init` lists the seeded `roadmap.md`, `breakdown.md`, and `evidence/step-1-1-demo.png`; the product PR body links the companion PR and contains "merge the companion PR first"; `node <agento-root>/scripts/agento.mjs doctor --root <tmp>/<product>` → `artifact-repo` `ok`; the second pass reports `nothing-to-migrate` and `gh pr list` shows no new PRs; trimmed outputs recorded in `evidence/step-6-1-migrate-smoke.md` (companion half) and the repo names added to `## Follow-ups` below.
- [ ] 6.2 Full-repository gate against the plan.md §5 baseline (190/190/0, shellcheck silent, both replays exit 0), recording exit codes and counts on this line: `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'`; `shellcheck scripts/hooks/delivery-guard.sh scripts/hooks/replay-guard.sh scripts/hooks/session-context.sh scripts/wait-for-checks.sh` (no output redirection); `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt`; `REPLAY_COMPANION=1 ./scripts/hooks/replay-guard.sh < tests/guard-fixtures-companion.txt` — verify: all four exit 0; `# fail 0`; `# tests` ≥ 190; no new or undocumented findings.
- [ ] 6.3 Integrate both defaults by merge (`git merge origin/main` in the product half; `git -C <half> merge origin/main` in the companion half), set `status: in-review` and `next-step: ""` in the roadmap (companion half), push product first then companion — verify: `git status --porcelain` empty in both halves; `git rev-list --count @{upstream}..HEAD` = 0 in both; `git merge-base --is-ancestor origin/main HEAD` exit 0 in both; `node scripts/agento.mjs initiative external-artifact-repo` lists `artifact-history-migration` as `in-review` with `errors: []`; `node scripts/agento.mjs session --pr` reports `companion.ahead: 0`, `companion.behind: 0`.
- [ ] 6.4 (manual, post-ship) In the prismicon primary window (default branch, clean), run `/agento agento-init --migrate`, accept the companion name `prismicon-docs`, merge the companion PR first and then the product PR, then run `/agento doctor` and screenshot the `artifact-repo` `ok` line naming `prismicon-docs` — verify: `evidence/step-6-4-prismicon-migration.png` linked from this line with the completion date; `gh repo view <owner>/prismicon-docs --json name -q .name` prints `prismicon-docs` (policy §4 exception accepted in plan.md Decisions Q5; landed by the ship epilogue via `post-ship/artifact-history-migration` in the companion).

## Follow-ups

- Hooks: the delivery guard's roadmap nudge inspects the companion **clone**'s index/`HEAD`, not the session's companion half, so paired-session product commits draw an `ask` even when the half's `HEAD` touched `roadmap.md`; re-target it to the half (same MANAGED_DIR rule the SessionStart hook already applies).
- `concurrent-delivery.instructions.md` (`applyTo: features/**,issues/**`) no longer loads for artifact edits made in the companion folder; consider shipping it (or a pointer) in the companion's `.github/instructions/` via init.
- Throwaway repositories from step 6.1 for the user to delete: `agento-smoke-migrate-<YYYYMMDD>` and `agento-smoke-migrate-<YYYYMMDD>-docs` (names filled in by the Builder).
