```yaml
status: complete
branch: feature/continue-command
last-updated: 2026-09-15
next-step: ""
initiative: "workflow-orchestration"
```

## Phase 1: CLI `next`

- [x] 1.1 Add `deriveNext({ role, worktree, delivery, lifecycle, owner, reviewFresh, candidates, requestedSlug, config })` to `scripts/session-state.mjs` implementing the plan.md §1 table (statuses `ok | none | ambiguous | blocked | unsupported | missing`; `next = { command, args, invocation, window, then, reason } | null`), never emitting `/agento ap` — verify: `node --test scripts/session-state.test.mjs` passes a table test covering every role × lifecycle × `reviewFresh`/owner cell, each non-`ok` status, slug mismatch in `build`/`plan`, and that every emitted command basename exists under `.github/prompts/`
- [x] 1.2 Wire `next [<slug>]` into `scripts/agento.mjs`: usage-header line; reuse the `session` composition; slug resolution (local roadmaps → `resolveRoadmapArtifact` remote fallback → unique ready initiative member → `missing`); `findOwner`; review freshness via `git log -1 --format=%ct <ref> -- <dir>/review.md` vs `-- . ':(exclude)<dir>/review.md'` with `<ref>` = `origin/<branch>` | local branch | `HEAD` (no fetch; warning when only the local branch exists); candidates (non-complete local roadmaps ∪ managed-worktree deliveries ∪ ready members); `dispatch { prompt, agent }` from `PLUGIN_ROOT` matching the prompt's `agent:` to an agent file's `name:`; exit 0 for `ok`/`none`, 3 otherwise, 1 on usage — verify: `node scripts/agento.mjs next` in this worktree prints `next.invocation: "/agento build-feature continue-command"`, `window: "here"`, and both `dispatch` paths exist; `node scripts/agento.mjs next --root /home/david/DP/agento` prints `/agento start-session feature/continue-command --resume` with `then: "/agento continue continue-command"`; `node scripts/agento.mjs` usage lists `next [<slug>]`
- [x] 1.3 Integration tests in `scripts/agento.test.mjs` over a clone + `git worktree add`: primary with one in-progress roadmap → start-session `--resume` + `then`; two in-flight roadmaps → `ambiguous`, exit 3, `candidates[].invocation`; build worktree `in-progress` → build-feature `here` with existing `dispatch` paths; `in-review` + fresh `approve` → ship `primary` (build worktree) and `here` (primary); stale `approve` (code commit after review.md) → review; fresh `request-changes` → build; `complete` with owner → ship; unplanned ready member slug from the primary → start-session + `then: /agento continue <f>`; detached `plan-*` worktree with one ready member → `new-feature initiative:<i>/<f>`; freehand → `unsupported` exit 3; wrong slug in a build worktree → `blocked` exit 3; unknown slug → `missing` exit 3 — verify: `node --test scripts/agento.test.mjs` passes
- [x] 1.4 Add `/agento continue` as the first `allowed` entry of every `primary`/`build`/`plan` row in `deriveAllowed`'s `TABLE` (freehand/unmanaged unchanged) and `continue: ["terminal", "ask-questions", "browser", "gh", "code", "network"]` to `COMMAND_NEEDS`; update the exact-row assertions in `scripts/session-state.test.mjs` (and `scripts/agento.test.mjs` / `tests/session-context.test.mjs` only where they break) — verify: `node --test scripts/session-state.test.mjs scripts/agento.test.mjs tests/session-context.test.mjs` passes; `node scripts/agento.mjs doctor --for continue` echoes the six needs; `node scripts/agento.mjs session` lists `/agento continue` first in `allowed`
- [x] 1.5 Integrate `origin/main` by merge, run the full gate, push — verify: `git merge origin/main` clean; `shellcheck scripts/hooks/delivery-guard.sh scripts/hooks/replay-guard.sh scripts/hooks/session-context.sh scripts/wait-for-checks.sh` exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0

## Phase 2: The `/agento continue` command

- [x] 2.1 Create `.github/prompts/continue.prompt.md` and the byte-identical `commands/continue.md` per plan.md §4: frontmatter (`description`, `argument-hint: "[<slug>]"`, `agent: "agent"`, no `tools:`), `Needs: terminal, ask-questions, browser, gh, code, network` / `Fallback:` lines, §9 receipt + idempotency sentence, `doctor --for continue` before the first write, `Window check per §11: requires role primary, plan, or build` (slug must equal `delivery.slug` in `build`/`plan`), the session → next → map-status → perform procedure (`here`: follow `dispatch.prompt` and `dispatch.agent` verbatim with `next.args`; `primary`/`secondary`: `code <path>` when available else the §10 fallback, then name the command), one transition per invocation, result `next:` = `then` or `/agento continue [<slug>]`; never `/agento ap`, never `worktree list --porcelain`, no policy canaries — verify: `diff .github/prompts/continue.prompt.md commands/continue.md` empty; `node --test tests/customizations.test.mjs` passes every prompt-structure, needs-table, doctor, and canary test for the new file (audit 2026-09-14: 16/18 pass; the two command-listing tests are satisfied by 2.2 and 2.3)
- [x] 2.2 Policy and instructions: add the `/agento continue` row to the §9 idempotency table (re-derives from git + roadmap state; performs the now-legal transition; never a second worktree, branch, or PR; the dispatched command's row governs the rest) and one §8 sentence naming `/agento continue [slug]` as the derived shorthand for the handoff commands; add `/agento continue` to the canonical list in `.github/instructions/command-invocation.instructions.md` — verify: `grep -n "/agento continue" .github/instructions/delivery-policy.instructions.md .github/instructions/command-invocation.instructions.md` shows §8, §9, and the list; `node --test tests/customizations.test.mjs` passes (2026-09-14: 17/18; the remaining README/docs listing test is 2.3's)
- [x] 2.3 Docs: `docs/commands.md` (table row, `## Invocation` entry, `next [<slug>]` in the CLI paragraph, the shorthand in "The standard flow" and "The initiative flow"); `README.md` (command reference row with window `primary · secondary`, a paragraph in "The full delivery flow"); `docs/architecture.md` mermaid node; `templates/AGENTS-section.md` and `.github/prompts/agento-init.prompt.md` ≡ `commands/agento-init.md` command lists; `AGENTS.md` scripts bullet gains `next`; `CHANGELOG.md` `## 0.4.0 (unreleased)` entry (no version bump) — verify: `grep -n "agento continue" README.md docs/commands.md docs/architecture.md templates/AGENTS-section.md commands/agento-init.md AGENTS.md CHANGELOG.md` shows each file; `diff .github/prompts/agento-init.prompt.md commands/agento-init.md` empty; `node --test tests/customizations.test.mjs` passes
- [x] 2.4 Integrate `origin/main` by merge, run the full gate, push — verify: `git merge origin/main` clean; shellcheck (explicit file list) exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0 (2026-09-14: already up to date; shellcheck 0; 140/140; replay 0)

## Phase 3: Rehearsal and finalization

- [x] 3.1 Rehearse the prompt against live state and record [`evidence/step-3-1-continue-rehearsal.md`](evidence/step-3-1-continue-rehearsal.md) (recorded 2026-09-14): for (a) the primary `--root /home/david/DP/agento` with no slug, (b) the primary with `continue-command`, (c) this promoted worktree while `in-progress` and again after a temporary `in-review` header (reverted, not committed), (d) a temporary freehand-named directory under the worktrees dir (removed afterwards) — paste each `agento.mjs next` JSON and the receipt, preflight, and result lines the prompt derives, then link the file from this step — verify: the file exists with four sections; re-running each recorded command reproduces the recorded `status`, `next.invocation`, and `window`; `git status --short` shows no leftover temporary changes
- [x] 3.2 Final gate against the plan.md `## Research` baseline, integrate `origin/main`, push, set `status: in-review` — verify: shellcheck (explicit file list) exit 0; `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'` 0 failures and total > 124; `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt` exit 0; `git diff --stat origin/main` touches only the files listed in plan.md `## Approach` "Files touched" (2026-09-14: shellcheck 0; 140 pass / 0 fail vs baseline 124; replay 0; 20 files, all listed)
