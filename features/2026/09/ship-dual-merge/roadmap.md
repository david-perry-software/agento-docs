```yaml
status: complete
branch: feature/ship-dual-merge
last-updated: 2026-09-17
next-step: ""
initiative: "external-artifact-repo"
```

## Phase 1: CLI — companion `behind`, `ship-preflight --pr`, lifecycle warning

- [x] 1.1 In `scripts/agento.mjs` `describeCompanion()`, read `behind` (`git -C <half> rev-list --count HEAD..@{upstream}`, `0` without an upstream or when unregistered) into the companion record and make `companionGaps()` append `"behind"` when `behind > 0`; extend the `close-decision` `companion-unpushed` message so a `behind`-only half reads `is behind its upstream; run git -C <path> merge origin/<branch> …`; extend the existing pair test in `scripts/agento.test.mjs` ("close-decision / ship-preflight companion") with a case that pushes a second commit to the companion's `origin/feature/widget` from the clone, fetches in the half, and asserts `companion.behind === 1`, `companionGaps` deep-equals `["behind"]`, `close-decision` exits 3 with `reason: companion-unpushed`, and after `git -C <half> merge origin/feature/widget` the gaps are `[]` again; keep every in-repo assertion (`companion: null`, `companionGaps: []`) — verify: `node --test scripts/agento.test.mjs` exit 0 with the new assertions; `node scripts/agento.mjs ship-preflight feature ship-dual-merge` in this worktree prints `"companion": null` and `"companionGaps": []`.
- [x] 1.2 In `scripts/agento.mjs` `case "ship-preflight"`, when `options.pr` is set call `lookupPullRequest(preflight.branch)` and `lookupCompanionPullRequest(preflight.branch)`, emit `pr`, `companionPr`, `warnings[]` (both lookups' warnings), and in companion mode append to `companionGaps[]`: `missing-pr` (companion lookup `null`), `pr-not-open` (`companionPr.state === "CLOSED"`), `conflicting-pr` (`companionPr.mergeStateStatus === "CONFLICTING"`); a `MERGED` companion PR adds no gap; without `--pr` the output is today's (no `pr`/`companionPr`/`warnings` keys); update the usage header line for `ship-preflight`; add tests using the `restrictedPath({ gh })` stub pattern from the `session --pr` tests: in-repo `--pr` → `pr.number === 15`, `companionPr === null`, one logged `gh` call; companion mode → both PRs, two calls, second `$PWD` in the clone; stub variants for `missing-pr`, `pr-not-open`, `conflicting-pr`, `MERGED` (no gap); no `--pr` → `!("pr" in json)` — verify: `node --test scripts/agento.test.mjs` exit 0 with the new tests listed as passing; `node scripts/agento.mjs ship-preflight feature ship-dual-merge --pr` in this worktree prints `"companionPr": null` and a `pr` object or a `pr:` warning.
- [x] 1.3 In `scripts/session-state.mjs` `deriveLifecycle({ delivery, pr, companionPr })` push `companion-pr-open: PR #<pr.number> for <branch> is merged but companion PR #<companionPr.number> is still open` when `pr?.state === "MERGED"` and `companionPr?.state === "OPEN"` (lifecycle unchanged; no warning when `companionPr` is `null`); pass `companionPr` from the `session` and `next` cases in `scripts/agento.mjs`; add `scripts/session-state.test.mjs` cases (warning present; absent when `companionPr` is `null`, `MERGED`, or `pr` is `OPEN`) and one `scripts/agento.test.mjs` `session --pr` companion-mode assertion where the stub `gh` answers `MERGED` in the product and `OPEN` in the clone — verify: `node --test scripts/session-state.test.mjs scripts/agento.test.mjs` exit 0 with the new cases; `node scripts/agento.mjs session --pr` in this worktree prints `"companionPr": null` and no `companion-pr-open` warning.

## Phase 2: Ship prompt — dual audit, dual merge, dual sync, companion epilogue

- [x] 2.1 Rewrite the companion-mode audit in `.github/prompts/ship.prompt.md`: resolve with `agento.mjs ship-preflight <type> <slug> --pr`; read `pr`, `companionPr`, `companion`, `companionGaps[]`; in companion mode (`companionPr !== null`, or `agento.mjs config` `artifactsRoot ≠ root` when `gh` degraded) read roadmap.md/review.md/plan.md from the companion's `origin/<branch>` via `git -C <artifactsRoot> fetch origin` + `git -C <artifactsRoot> show origin/<branch>:<path>`, keep the code diff on the product; add the new gap tokens (`behind`, `missing-pr`, `pr-not-open`, `conflicting-pr`) to the hard-reject list naming `companion.path` and the companion PR URL; when the companion PR is `BEHIND` its default, `git -C <companion.path> merge origin/<default>` (abort + reject on conflict, mirroring the product rule); add the §9 idempotency case "`pr.state === MERGED` and `companionPr.state === OPEN` → skip audit writes and the code merge, resume at 2.2's companion ready/wait/merge"; copy to `commands/ship.md` — verify: `grep -c 'companionPr' .github/prompts/ship.prompt.md` ≥ 3; `grep -c 'ship-preflight <type> <slug> --pr\|ship-preflight.*--pr' .github/prompts/ship.prompt.md` ≥ 1; `cmp .github/prompts/ship.prompt.md commands/ship.md` silent; `node --test tests/customizations.test.mjs` exit 0.
- [x] 2.2 Rewrite step 3 of `ship.prompt.md` for companion mode: commit `status: complete` (+ `## Follow-ups (accepted at ship)`) in the companion half with `git -C <companion.path>` and push it (changelog stamp stays a product commit); `gh pr ready <n>` in the product and `gh pr ready <m>` run from inside the companion clone (`cd <artifactsRoot> && gh …`, or `--repo $(gh repo view --json nameWithOwner -q .nameWithOwner)` derived there — never `--repo <artifacts.repo.name>`); `scripts/wait-for-checks.sh pr <n>` then merge the code PR (normal merge commit, delete remote branch); then `scripts/wait-for-checks.sh pr <m> --repo <nameWithOwner>` and merge the companion PR the same way from inside the clone; a companion merge failure is a resumable hard stop ending with `Result: failed — code PR #<n> merged, companion PR #<m> open at <url>; re-send /agento ship <slug> to resume at the companion merge`; state the ordering rationale in one sentence (code checks are the gate; the companion carries artifacts); sync both defaults (product as today; companion `git -C <artifactsRoot> switch <default>`, `fetch --prune`, `merge --ff-only origin/<default>`, verify clean and zero ahead/behind); teardown unchanged, plus `git -C <artifactsRoot> branch -d <branch>` for the merged companion local branch; in-repo mode stays byte-for-byte today's flow; copy to `commands/ship.md` — verify: `grep -c 'ff-only' .github/prompts/ship.prompt.md` ≥ 1; `grep -c -- '--repo <artifacts.repo.name>' .github/prompts/ship.prompt.md` = 0; `grep -c 'resume at the companion merge' .github/prompts/ship.prompt.md` ≥ 1; `cmp .github/prompts/ship.prompt.md commands/ship.md` silent; `node --test tests/customizations.test.mjs` exit 0.
- [x] 2.3 Rewrite step 5 (post-ship epilogue) and step 4 (report) of `ship.prompt.md`: in companion mode the `post-ship/<slug>` branch, evidence + roadmap-tick commit, PR, merge, branch delete, and default sync happen in the companion repository (`git -C <artifactsRoot>` from its fresh default; `gh` from inside the clone); the report names both merged PR numbers, both synced defaults, and the removed halves + workspace file; in-repo unchanged; copy to `commands/ship.md` — verify: `grep -c 'post-ship' .github/prompts/ship.prompt.md` ≥ 3; `grep -c 'both' .github/prompts/ship.prompt.md` ≥ 2; `cmp .github/prompts/ship.prompt.md commands/ship.md` silent; `node --test tests/customizations.test.mjs` exit 0.

## Phase 3: Policy and docs

- [x] 3.1 Add the Decision Q2 case to the `/agento ship` row of `.github/instructions/delivery-policy.instructions.md` §9 idempotency table (`code PR merged while the companion PR is still open resumes at the companion merge, then sync, teardown, epilogue`); no new `§` sections — verify: `grep -c 'companion' .github/instructions/delivery-policy.instructions.md` greater than `git show origin/main:.github/instructions/delivery-policy.instructions.md | grep -c 'companion'`; `node --test tests/customizations.test.mjs` exit 0.
- [x] 3.2 Update `docs/commands.md` (the `ship-preflight` paragraph: `--pr` adds `pr`, `companionPr`, `warnings[]`; `companion` gains `behind`; `companionGaps[]` tokens `dirty`, `unpushed`, `behind`, `missing-pr`, `pr-not-open`, `conflicting-pr`; replace the "Interim limitation" sentence with the dual-merge flow; `session`'s `companion-pr-open` warning), `README.md` `### 5. Ship — primary window` (one companion-mode paragraph: both PRs ready, code first then companion, both defaults synced, epilogue in the companion, resume-at-companion-merge on failure), and `CHANGELOG.md` `## Unreleased` (new **Ship dual merge** bullet; drop the "Interim: … until `ship-dual-merge` lands" sentence from the mirrored-branches bullet) — verify: `grep -c 'Interim limitation' docs/commands.md` = 0; `grep -c 'leaves the companion PR open' CHANGELOG.md` = 0; `grep -c 'Ship dual merge' CHANGELOG.md` = 1; `grep -c 'behind' docs/commands.md` ≥ 1; `grep -c 'companion' README.md` greater than `git show origin/main:README.md | grep -c 'companion'`; `node --test tests/customizations.test.mjs` exit 0.

## Phase 4: End-to-end drive and gate

- [x] 4.1 Drive the rewritten ship prompt's exact command sequence on a throwaway `/tmp` product + companion pair (bare origins, product `.github/agento.json` with `artifacts.repo.name`, a build pair on `feature/xw` with a companion roadmap `status: in-review` + `review.md` `Verdict: approve` + `artifact-pr: "#7"`), with a stub `gh` on `PATH` that logs `$PWD $*` to a file and answers `pr view` from a state file — run A: clean (both `pr merge` succeed, states flip to `MERGED`); run B: companion `pr merge` fails once, then re-send with `pr` `MERGED` / `companionPr` `OPEN` resumes at the companion merge — and record `ship-preflight --pr` output at each stage, the `gh` call log, and `git -C` worktree lists before/after teardown as `features/2026/09/ship-dual-merge/evidence/step-4-1-dual-merge-drive.txt` — verify: the log shows the product `pr merge` before the companion `pr merge`, every companion `gh` call with `$PWD` inside the companion clone, run B's second pass containing no product `pr merge`; both defaults end clean with zero ahead/behind; both halves and the `.code-workspace` file are gone; the evidence file is committed on this branch. Done 2026-09-17: [evidence/step-4-1-dual-merge-drive.txt](evidence/step-4-1-dual-merge-drive.txt) — drive exit 0; run A `pr merge 12` (log line 40) before `pr merge 7` (line 71); run B's pass 2 (after the log marker) contains only companion calls, `pr merge 7` twice total; every `pr ready 7`/`pr merge 7`/`repo view` ran with `$PWD` in the clone, the `wait-for-checks.sh pr 7` `pr view` carried `--repo acme/project-docs`; `session --pr` in the half-shipped state warned `companion-pr-open`; both defaults `status []`, `0/0` ahead/behind, `feature/xw` gone locally and remotely, both worktree lists reduced to the primary, `wt/` and `project-docs-worktrees/` empty; `wait-for-checks.sh` worked unmodified against the stub.
- [x] 4.2 Full-repository gate against the plan.md §5 baseline (188/188/0, shellcheck silent, both replays exit 0), recording exit codes and counts on this line: `node --test 'scripts/**/*.test.mjs' 'tests/**/*.test.mjs'`; `shellcheck scripts/hooks/delivery-guard.sh scripts/hooks/replay-guard.sh scripts/hooks/session-context.sh scripts/wait-for-checks.sh` (no output redirection — the guard denies it beside hook paths); `./scripts/hooks/replay-guard.sh < tests/guard-fixtures.txt`; `REPLAY_COMPANION=1 ./scripts/hooks/replay-guard.sh < tests/guard-fixtures-companion.txt` — verify: all four exit 0; `# fail 0`; `# tests` ≥ 188; no new or undocumented findings. Done 2026-09-17: `node --test` exit 0 — `# tests 190`, `# pass 190`, `# fail 0` (baseline 188/188/0; +2 are this delivery's tests); `shellcheck` exit 0, no output; in-repo replay exit 0; companion replay exit 0 — no new or undocumented findings.
- [x] 4.3 Integrate `origin/main` by merge, set `status: in-review` and `next-step: ""`, push — verify: `git status --porcelain` empty; `git rev-list --count @{upstream}..HEAD` is 0; `git merge-base --is-ancestor origin/main HEAD` exit 0; `node scripts/agento.mjs initiative external-artifact-repo` lists `ship-dual-merge` as `in-review` with `errors: []`. Done 2026-09-17: `git merge origin/main` → already up to date (ancestor); status set, pushed; verification recorded in the commit's push output.

## Follow-ups

- `session --pr` and `ship-preflight --pr` each run `gh --version` once per lookup; a shared probe would save a spawn per call.
- `artifact-history-migration` (initiative member): decide whether `status` should walk registered companion halves; it is the first real companion-mode exercise of the dual merge on GitHub.
